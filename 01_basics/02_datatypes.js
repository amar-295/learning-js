"use strict"; //treat all JS code as newer version

// alert("Hello JS") we are using Node.js not browser

console.log(3+3); // gives the value 6

// code readability should be high

let name = "Amarnath" // String
let age = 20 // int
let isTrue = true // boolean => true or false

// null => standalone value(it is also a datatype)

// undefines => not assinged value (let state;)

// symbol => used for uniqness


// object

console.log(typeof undefined); // undefined
console.log(typeof null); // object 
